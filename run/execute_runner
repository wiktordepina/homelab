#!/bin/bash

docker run --rm \
  --name runner \
  -v .:/build \
  -w /build \
  -e PM_USER="${PM_USER}" \
  -e PM_PASS="${PM_PASS}" \
  runner-toolbox \
  "${@}"

  # -v /pve/terraform:/pve/terraform \