#!/bin/bash

docker run --rm \
  --name runner \
  -v /pve/terraform:/pve/terraform \
  -v /home/runner/.ssh:/root/.ssh \
  -v .:/build \
  -w /build \
  -e PM_USER="${PM_USER}" \
  -e PM_PASS="${PM_PASS}" \
  -e DNS_TSIG_KEY="${DNS_TSIG_KEY}" \
  runner-toolbox \
  "${@}"
