[Unit]
Description=prometheus
Requires=network.target 
After=network.target

[Service]
Type=simple
WorkingDirectory=/etc/prometheus
ExecStart=/etc/prometheus/prometheus --config.file='/etc/prometheus/prometheus.yaml' --storage.tsdb.path='/prometheus_data/'
Restart=always
User=root

[Install]
WantedBy=multi-user.target
