{# Environment file loaded by the systemd unit. Each variable is sourced from
   /pve/secrets/hermes.sh on the runner via ansible.builtin.env lookup. #}
HERMES_LITELLM_KEY={{ lookup('ansible.builtin.env', 'HERMES_LITELLM_KEY') }}
