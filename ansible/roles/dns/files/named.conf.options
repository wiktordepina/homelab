options {
  directory "/var/cache/bind";
  listen-on { localnets; };
  allow-transfer { none; };
  allow-query { localnets; };
  allow-query-cache { none; };
  recursion no;
  dnssec-validation auto;
};
