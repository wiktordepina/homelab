---
version: "3.8"

services:
  pontainer:
    restart: always
    image: portainer/portainer-ce:latest
    ports:
      - "9443:9443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - pontainer_data:/data

volumes:
  pontainer_data:
